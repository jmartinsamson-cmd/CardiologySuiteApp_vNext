# Azure AI Search Configuration
# ================================
# All Azure AI Search traffic routes to: cardiologysuite-search-pro
#
# Required Environment Variables:
# - AZURE_SEARCH_ENDPOINT: Full endpoint URL to the Azure AI Search service
# - AZURE_SEARCH_API_KEY: API key for authentication (Admin key for write operations, Query key for read-only)
# - AZURE_SEARCH_INDEX: Name of the search index to query
#
# Optional:
# - AZURE_SEARCH_SERVICE_NAME: Service name only (will construct endpoint if AZURE_SEARCH_ENDPOINT not set)
# - AZURE_SEARCH_API_VERSION: API version to use (defaults to 2024-07-01)

# Primary Azure Search Service (cardiologysuite-search-pro)
AZURE_SEARCH_ENDPOINT=https://cardiologysuite-search-pro.search.windows.net
AZURE_SEARCH_SERVICE_NAME=cardiologysuite-search-pro
AZURE_SEARCH_INDEX=cardiology-index
AZURE_SEARCH_API_KEY=<READ_FROM_SECRET_MANAGER>
AZURE_SEARCH_API_VERSION=2024-07-01

# Legacy variable names (deprecated but still supported for backward compatibility)
# AZURE_SEARCH_NAME=cardiologysuite-search-pro
# AZURE_SEARCH_ADMIN_KEY=<READ_FROM_SECRET_MANAGER>

# Azure OpenAI Configuration (for RAG/QA features)
AZURE_OPENAI_ENDPOINT=https://your-openai.openai.azure.com
AZURE_OPENAI_API_KEY=<READ_FROM_SECRET_MANAGER>
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4
AZURE_OPENAI_API_VERSION=2024-02-01

# Application Settings
NODE_ENV=development
PORT=8081

# IMPORTANT: Never commit actual API keys to version control
# Use Azure Key Vault, GitHub Secrets, or other secret management solutions

# ============================================================================
# ACTUAL KEYS (for reference - DO NOT commit these in production)
# ============================================================================
# Admin Key (for index creation, document upload, etc.):
#   - Stored in Azure Key Vault as: cardiologysuite-search-pro-admin-key
#   - Retrieve with: az keyvault secret show --name cardiologysuite-search-pro-admin-key --vault-name <your-vault>
#   - Set locally as: AZURE_SEARCH_API_KEY=<admin-key>
#
# Query Key (read-only, for search queries):
#   - Stored in Azure Key Vault as: cardiologysuite-search-pro-query-key  
#   - Retrieve with: az keyvault secret show --name cardiologysuite-search-pro-query-key --vault-name <your-vault>
#   - Set locally as: AZURE_SEARCH_API_KEY=<query-key>
#
# For local development, create a .env file (ignored by git) and add:
#   AZURE_SEARCH_API_KEY=<your-actual-key-here>
